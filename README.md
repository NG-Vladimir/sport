# Fit Track — SPA для фитнеса

Современное веб-приложение (SPA) для фитнеса на **React/Next.js** с **Tailwind CSS**, в стиле Nike Run Club / Training Club.

## Стек

- **Next.js 14** (App Router)
- **React 18**, **TypeScript**
- **Tailwind CSS** — тёмная тема, неон/салатовые акценты
- **Framer Motion** — плавные анимации
- **Lucide React** — иконки
- **date-fns** — даты
- **LocalStorage** — сохранение прогресса

## Запуск

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000).

## Структура проекта

```
src/
├── app/                    # Страницы (App Router)
│   ├── page.tsx             # Dashboard
│   ├── log/page.tsx         # Лог тренировки
│   ├── achievements/page.tsx
│   └── history/page.tsx
├── components/
│   ├── WorkoutCard.tsx      # Карточка тренировки
│   ├── ProgressBar.tsx     # Прогресс-бар
│   ├── AchievementBadge.tsx
│   ├── ExerciseInput.tsx   # Ввод повторений
│   ├── ActivityRings.tsx   # Кольца активности
│   └── Nav.tsx
├── context/
│   └── ProgressContext.tsx  # Состояние + LocalStorage
├── lib/
│   ├── workoutPlan.ts      # Алгоритм 90-дневного плана
│   ├── storage.ts          # XP, уровни, сохранение
│   ├── achievements.ts     # Достижения
│   └── xp.ts               # Расчёт XP за тренировку
└── types/
    └── index.ts
```

## Логика тренировок (90 дней)

- **Дни:** Пн, Ср, Пт.
- **Исходные максимумы:** подтягивания 7, приседания 40, пресс 30, отжимания 30.
- **Прогрессия:** +2–5% к объёму в неделю (прогрессивная перегрузка).
- **Чередование:** сила → выносливость → взрывная.
- **Проверка максимума:** раз в 2 недели (каждые 14-й тренировочный день).

## Функционал

1. **Dashboard** — сегодняшняя тренировка, уровень/XP, кольца активности, статистика.
2. **Лог** — ввод повторений по подходам, сохранение тренировки.
3. **Достижения** — бейджи (первая сотня приседаний, король турника, 30 дней без пропусков и др.).
4. **История** — календарь с отметками выполненных тренировок.
5. **Геймификация** — уровни и XP за каждую тренировку и достижения.

## Дизайн

- Тёмная тема, акценты: неон/салатовый (`#b8ff3c`), зелёный, голубой, розовый.
- Жирные шрифты (Oswald для заголовков).
- Карточный интерфейс, анимации Framer Motion.
- Кольца активности на дашборде.

Данные хранятся в **LocalStorage** — прогресс сохраняется между сессиями.
